@model BackOffice.Models.LenguajeModel

<h2>Actor</h2>

@using (Html.BeginForm("AddLenguaje", "Pelicula", FormMethod.Post))
{
    <div>
        <fieldset>
            <legend>Agregar Director</legend>
            <div class="editor-label">
                @Html.LabelFor(m => m.Nlenguaje)
            </div>
            <div class="editor-field">
                @Html.TextBoxFor(m => m.Nlenguaje)
                @Html.ValidationMessageFor(m => m.Nlenguaje)
            </div>
            <p>
                <input type="submit" value="Agregar" />
            </p>
        </fieldset>

    </div>
    
}
